extends layout

block content

  h1 #{title}: #{book_instance[0]._id}

  p Do you really want to delete this instance?

  form(method='POST')
      div.form-group
      input#instanceid.form-control(type='hidden', name='instanceid', value=book_instance[0]._id )

      button.btn.btn-primary(type='submit') Delete
